"use client";

import Script from "next/script";
import { META_PIXEL_ID } from "@/lib/meta-pixel-shared";

// Base do Meta Pixel (PageView automático) — montado só nas páginas
// PÚBLICAS do site (ver AppChrome.jsx: nunca dentro de /admin, para não
// enviar a navegação da própria equipe pro pixel). O evento de conversão em
// si (Lead) é disparado à parte, no momento real do cadastro — ver
// lib/meta-pixel-client.js, chamado por SimulationForm/QuickAttendanceForm.
export default function MetaPixel() {
  if (!META_PIXEL_ID) return null;

  return (
    <>
      <Script id="meta-pixel-base" strategy="afterInteractive">
        {`!function(f,b,e,v,n,t,s)
{if(f.fbq)return;n=f.fbq=function(){n.callMethod?
n.callMethod.apply(n,arguments):n.queue.push(arguments)};
if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';
n.queue=[];t=b.createElement(e);t.async=!0;
t.src=v;s=b.getElementsByTagName(e)[0];
s.parentNode.insertBefore(t,s)}(window, document,'script',
'https://connect.facebook.net/en_US/fbevents.js');
fbq('init', '${META_PIXEL_ID}');
fbq('track', 'PageView');`}
      </Script>
      <noscript>
        <img
          height="1"
          width="1"
          style={{ display: "none" }}
          src={`https://www.facebook.com/tr?id=${META_PIXEL_ID}&ev=PageView&noscript=1`}
          alt=""
        />
      </noscript>
    </>
  );
}
